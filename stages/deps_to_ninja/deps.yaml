---
build:
  copy_files:
    - tuscan/ninja_syntax.py
    - tuscan/utilities.py
    - tuscan/provides.json

run:
  dependencies:
    stages:
      - get_base_package_names
    data_containers:
      - tuscan_data
      - sources

  stdout: build.ninja
  stderr: deps
